# Intuition On Convolution Operation

- **Repo_Name:** `learn-by-rebuilding`
- **Category:** `dlcv`
- **Project:** `intuition-convolution-operation`
- **Project Root Path:** `learn-by-rebuilding/dlcv/intuition-convolution-operation`

## Article Used
Primary content is not taken from any other article

## Overview
This project implements and experiments mainly with the convolution operation itself.  

The goal is to explore what convolution operation exactly does with the input image. 

---

## Objectives

- Creating a dataset manually with B&W images of basic shapes like line, circle, etc
- Applying different hand-crafted kernels and perform convolution operation over the images
- Plotting input image and convolved output image for comparable visualization
- Exlporing different kernels and the functionalities of those kernels

---

## Project Structure

Here is the directory structure:
```
data-extraction/docling-document-alchemist/
├── readme.md
├── .gitignore
├── experiments.ipynb
└── requirements.txt
```

---

## Description of Code files
text

## How to run?

- Create one venv using `python -m venv .mlenv` command in WSL terminal.
- Activate this with `source .mlenv/bin/activate` command.
- Create a `requirements.txt` file and pip install libraries by running `pip install -r requirements.txt`.
- Notebooks are ready to execute. 
